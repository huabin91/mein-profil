function getWorkspaceDetails() {
    let workspaceName = document.querySelector('.db');
    console.log(workspaceName);
}

window.addEventListener('load', getWorkspaceDetails);

window.onload = getWorkspaceDetails;